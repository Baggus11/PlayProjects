namespace CWClassMapGenerator
{
    public interface IMappable
    {
        //Map A->B
        //A Map<A, B>(A source, B destination);
        //void Map<A, B>(ref A source, ref B destination);
    }
}
