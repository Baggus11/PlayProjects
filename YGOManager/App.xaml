<Application
    x:Class="YugiohCardDownloader.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:YugiohCardDownloader"
    StartupUri="YGOManagerMainView.xaml">
    <Application.Resources>
        <Style x:Key="basicStyle">
            <Setter Property="Control.FontFamily" Value="Monda" />
            <Setter Property="Control.FontSize" Value="16" />
        </Style>
        <Style BasedOn="{StaticResource basicStyle}" TargetType="{x:Type Label}">
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style BasedOn="{StaticResource basicStyle}" TargetType="{x:Type TextBlock}">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style BasedOn="{StaticResource basicStyle}" TargetType="{x:Type Button}">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="2,4" />
        </Style>
        <Style BasedOn="{StaticResource basicStyle}" TargetType="{x:Type GroupBox}" />
    </Application.Resources>
</Application>
